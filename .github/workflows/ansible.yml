---
name: CI
on:
  push:
  workflow_dispatch:
jobs:
  ansible-role:
    uses: punktDe/shared-workflows/.github/workflows/ansible-role.yml@refactor
    secrets:
      galaxy_api_key: ${{ secrets.GALAXY_API_KEY }}
